{{- if .Values.eventProcessor.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.eventProcessor.serviceAccount.name }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "cloudops-ai.labels" . | nindent 4 }}
    app.kubernetes.io/component: event-processor
  {{- with .Values.eventProcessor.serviceAccount.iamRoleArn }}
  annotations:
    eks.amazonaws.com/role-arn: {{ . | quote }}
  {{- end }}
{{- end }}